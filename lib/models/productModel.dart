class ProductModel {
  String title;
  String description;
  String manufactor;
  double cost;
  String imageURL;

  ProductModel(
      {required this.title,
      required this.description,
      required this.manufactor,
      required this.cost,
      required this.imageURL});
}
